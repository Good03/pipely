# pipely

Before start you need to create a personal token in Azure DevOps

User Settings -> Personal Access Token -> New Token -> Save the token somewhere safe, you will need it later.

Add the execution file to your PATH variable, so you can run it from anywhere in the terminal.
